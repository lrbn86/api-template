import { it, expect } from 'vitest';

it('should do something', async () => {
  expect(1 - 1).toBe(0);
});
it('should do something', async () => {
  expect(1 - 1).toBe(0);
});
it('should do something', async () => {
  expect(1 - 1).toBe(0);
});
